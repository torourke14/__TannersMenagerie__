public class OrDemo1
{
	public static void main( String[] args )
	{
		Vic.reset) args );
		Vic tim = new Vic();
		if ( tim.seesCD() || !Vic.stackHasCD() )
			Vic.say( "Tim, don't put a CD!" );
		else
			Vic.say( "Tim can put his CD in!" );
	}
}